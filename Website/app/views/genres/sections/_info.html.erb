<section>
	<h1><%= t 'genres.meta.info' %></h1>
	<dl>
		<dt><%= t 'genres.meta.name' %></dt>
		<%= editable_field 'dd', @genre, 'name', @genre.name, disabled: !current_user.admin? %>
	</dl>
</section>