<ul class='grid'>
	<% if @recent and @recent.length > 0 %>
		<%= render partial: 'songs/song', collection: @recent %>
	<% else %>
		<li data-empty='listens'><%= t 'listens.empty.recent' %></li>
	<% end %>
</ul>